source 'https://rubygems.org'

git_source(:gitnhub) do |repo_name|
  repo_name = "#{repo_name}/#{repo_name}" unless repo_name.include?('/')
  "https://github.com/#{repo_name}.git"
end

# slack
gem 'slack-api'
#http
gem 'http'
#json
gem 'json'
#gtk
gem 'gtk3'
